using UnityEngine;
using System.Collections;

public class PrintDisplayData : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log (Display.displays[0].systemHeight);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
